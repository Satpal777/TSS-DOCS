'use client';

import 'tea-simple-smart-css';

/**
 * TSSInitializer component
 * This component handles the client-side initial scan of the DOM.
 * Because it's marked with 'use client', Next.js will bundle the library
 * and execute its side-effects (scanning the document) on the client.
 */
export default function TSSInitializer() {
  return null;
}
